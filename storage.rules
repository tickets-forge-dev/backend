rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Workspace isolation for file uploads
    match /workspaces/{workspaceId}/{allPaths=**} {
      allow read, write: if request.auth != null &&
        request.auth.token.workspaceId == workspaceId;
    }
  }
}
