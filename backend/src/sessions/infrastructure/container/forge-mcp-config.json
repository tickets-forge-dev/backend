{
  "mcpServers": {
    "forge": {
      "command": "node",
      "args": ["/home/user/forge-mcp-server/dist/index.js"],
      "env": {
        "FORGE_API_URL": "{{FORGE_API_URL}}",
        "FORGE_SESSION_JWT": "{{FORGE_SESSION_JWT}}",
        "TICKET_ID": "{{TICKET_ID}}"
      }
    }
  }
}
